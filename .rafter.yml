# Live-probe config for sb-u14j / PR #53 (e158bb7).
# Corrected per Mayor's audit: R-310C9 is the id that actually appears in
# the report for the insecure-random-for-security finding (my earlier
# R-91AFF was a self-inflicted hash-preimage error, not a backend result).
# excludePaths kept in camelCase form to re-confirm that half against a
# stable revision pair (00260-kwf for both C and D).
scan:
  excludePaths:
    - "token.py"
  ignore:
    - paths: ["**"]
      rules: ["R-310C9"]
      reason: "probe sb-u14j: suppressing insecure-random-for-security on app.js via corrected R-XXXXX display id"
